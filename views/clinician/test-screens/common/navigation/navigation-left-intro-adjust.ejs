<div class="card-header">
    <div class="row" id="introBack">
        <div class="col-3"><button id="<%= back_id %>" type="button" class="btn btn-outline-primary btn-sm btn-block">Back</button></div>
        <div class="col-9"><h4 style="font-style:italic"><span style="font-weight:bold;color:slateblue"><%= session_name %></span> - <span style="font-weight:bold;color:red"><%= test_name %></span></h4></div>        
    </div>
</div>
<div class="card-body">
    <div class="form-group">
        <div class="form-check">
            <input id="<%= checkbox_id %>" type="checkbox" class="form-check-input" <%= record_checked %> style="-ms-transform:scale(2);-moz-transform:scale(2);-webkit-transform:scale(2);-o-transform:scale(2);padding:20px;" >
            <label class="form-check-label" style="font-weight:bold;font-style:italic">&nbsp;&nbsp;<%= record_name %></label>
        </div>
    </div>        
    <div class="form-group row">
        <label class="col-4 col-form-label" for="selectLanguage" style="font-weight:bold;font-style:italic">Language:</label>
        <div class="col-5">
            <select class="form-control" id="selectLanguage">
            <% 
                if (languages.includes('fr')) {
            %>
                <option value='fr' <%= default_language == 'fr' ? 'selected' : '' %>>French</option>
            <%
                }

                if (languages.includes('de')) {
            %>

                <option value='de' <%= default_language == 'de' ? 'selected' : '' %>>German</option>
            <%
                }

                if (languages.includes('es')) {
            %>
                <option value='es' <%= default_language == 'es' ? 'selected' : '' %>>España</option>
            <%
                }
            %>
            </select>
        </div>
    </div>
    </form>
</div>
<div class="card-footer" id="runTest">
    <button id="Run_<%= run_button_id %>" type="button" class="btn btn-info btn-lg btn-block">Run</button>                
</div>